import React from 'react'
import { motion, AnimatePresence } from "framer-motion";
import { useSnapshot } from 'valtio';
import {
    headContainerAnimation,
    headContentAnimaton, headTextAnimation, slideAnimation
} from '../config/motion'

const Home = () => {
  return (
    <div>Home</div>
  )
}

export default Home